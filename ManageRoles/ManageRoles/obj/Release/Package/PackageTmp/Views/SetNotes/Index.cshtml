@model IEnumerable<ManageRoles.Repository.SetNote>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutSuperAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Setnotename)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Setnotename)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SetID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SetID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SetID })
        </td>
    </tr>
}

</table>
